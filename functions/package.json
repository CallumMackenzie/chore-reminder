{
  "name": "chore-reminder-functions",
  "version": "0.1.0",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "rm -rf lib && tsc",
    "deploy": "firebase deploy --only functions,firestore:rules",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2",
    "twilio": "^6.0.2"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^4.1.10"
  }
}
